#!/bin/bash

cwd=`pwd`
openjdk_dir=`ls $cwd | grep jdk9`

#echo ">>>>>openkdk_dir=$openkdk_dir"

if [ ! -d "$openjdk_dir" ]; then
	echo "No OpenJDK sources! To get the sources run: sh ./get_source.sh"
	exit
fi

cd ./${openjdk_dir}
#echo "Command: bash ./configure $@"
bash ./configure "$@"


